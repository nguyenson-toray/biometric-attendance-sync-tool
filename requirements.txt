# Core dependencies for biometric-attendance-sync-tool
# Note: These are already installed in frappe-bench environment
# Only needed if running standalone

requests>=2.25.0
pickledb>=1.3.0
pyzk>=0.9

# Required: Vietnamese name normalization for device compatibility
# Converts 'Nguyễn Văn A' → 'Nguyen Van A' for biometric devices
unidecode>=1.2.0
